package project.mechanics;

/**
 * Created by Nils Broman.
 * All classes with this interface implements a function boardChanged, that is called whenever the playing fiels of the game
 * is changed.
 */
public interface BoardListener {
    void boardChanged();
}
